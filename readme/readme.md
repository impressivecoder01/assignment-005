A.getElementById → returns one element by ID. B.getElementsByClassName → returns a live list of elements with that class. C.querySelector → returns first match using CSS selector. D. querySelectorAll → returns a static list of all matches.
create anyElement and then use appendChild(that element)
The event starts by the clicked element and moves up to its parent elements.
Attach one event listener to a parent to handle events from many children. Useful because it saves memory and works for new elements too.
A. preventDefault() → stops default action (like form submit). B.stopPropagation() → stops event from bubbling up.